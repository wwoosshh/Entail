"""The start-up hook lives in sitecustomize.py (see its docstring). This file makes the directory a package so the
wheel ships the hook; putting the directory itself on PYTHONPATH still works for a source checkout."""
